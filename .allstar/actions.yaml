optConfig:
  optIn: true
action: issue
groups:
  name: "test"
  repos:
    - name: "*"
  rules:
    - name: "ci-test"
      method: "require"
      requireAll: true
      mustPass: false
      actions:
        - name: "test-action"
    
